[phases.setup]
nixPkgs = ["nodejs", "python311"]

[phases.install]
cmds = ["npm ci", "python -m venv --copies /opt/venv", "/opt/venv/bin/pip install -r requirements.txt"]

[phases.build]
cmds = ["npm run build"]
