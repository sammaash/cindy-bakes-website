[build]
builder = "NIXPACKS"

[deploy]
startCommand = "/opt/venv/bin/gunicorn --bind 0.0.0.0:$PORT 'whatsapp_webhook:create_app()'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
